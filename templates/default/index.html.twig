{% extends 'base.html.twig' %}

{% block jumbo %}
    <div class="container">
            <h1>{{ homepage.title }}</h1>
    </div>
{% endblock %}

{% block styles %}
<style>
.jumbotron {
    background-image:
    linear-gradient(
        rgba(0, 0, 0, 0.2),
        rgba(0, 0, 0, 0.2)
    ),
    url('{{ asset('img/Weave-Ball.jpg') }}');
}
</style>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            {{ homepage.content|raw }}
        </div>
    </div>
{% endblock %}
